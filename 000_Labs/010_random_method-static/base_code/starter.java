/*
 *	Author:  
 *  Date: 
*/

import java.util.Scanner;
import java.util.Random;

class starter {
	public static void main(String args[]) {
		// the string "I love to learn coding remotely." will appear in
		// the command window when you compile and run this program.
		int x = (int)(Math.random() * 10);
		System.out.println("A number between the numbers 0-9: " + x); 
		int y = (int)(Math.random() * 10)+1;
		System.out.println("A number between the numbers 1-10: " + y);
		double z = (Math.random() * 10)+2.5;
		System.out.println("A number between the numbers 2.5-3.5: " + z);
		double i = (Math.random() * 10)+14;
		System.out.println("A double between the numbere 14-589: "+ i);



		
	}
}
